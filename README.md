# Engineering-Tool
In order to run this application you will need to install the libraries in the file requirments.txt
You will also need to use either Python 3.12 or Python 3.13

To get started:
1. Clone the repository.
2. Create a virtual environment:
   ```bash
   python -m venv .venv
3. activate vevnv
   ```python
   .venv\scripts\activate 
   